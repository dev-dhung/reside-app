import 'dart:typed_data';

Future<void> saveAndShareFile(
  Uint8List bytes,
  String filename,
  String mimeType,
) async {
  throw UnimplementedError(
    'No platform implementation available for saveAndShareFile.',
  );
}
